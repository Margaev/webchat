apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: web
resources:
  - web-deployment.yaml
  - web-service.yaml
images:
  - name: kind-registry:5000/web
    newName: kind-registry:5000/web
